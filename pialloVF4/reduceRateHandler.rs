when local function = localScript = setTrueEverytime = true 
    then let function print goDown 
        (local 
function while software.Running = setTrue = true 
then let functioningSoftware.connectIntoChain.connect = setTrue = true 
   AfterFunction = setSoftware.Running.mustBeTrue goDown
   when goUp (CheckUp.readUp) = try.SetTrue = true 
   then as runningSoftware = true 
if let softwareRun.PIALLO then setTrueEverytime = outBracket.force = true (setTrueEverytime)
if detectSoftware.Running = true as setBefore.Function = AfterFunction.reduceRates = ping 
and reduceRates = ms = true then outBracket = (setTrueEverytime)
then let function.endInstant.end 
})
after function.endInstant.end)
end) 

when function = reduceHandler.localServers.localClient.client.instantDetect 
    when instantClient.localClientAndServer = workTogether = setTrue goOutBracket = true (setTrueEverytime) goDown 
    then readDown let setTrue = true 
        when function = workAs.runningSoftware = setRunning = true 
            then let print ("Advanced ping reducer is now running.")
                if goUp.asChain.checkUp.readUp = true 
                as try.true
                then endInstant.end)
            end)
        end)
    end)
}