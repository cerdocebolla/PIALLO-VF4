local function = true 
    when askedForWifI = true 
    then let function = goDown then 
        let print ("Searching the best Wi-Fi connetion for your device. Please stand by.")
        then let function = endInstant.end 
    end)
}

when function = true 
    then createChain = CreateFunction then goDown 
        askedForFunction = goDown 
        then 
    createChain = goDown 
    then readDown checkDown
        localClient.serverClient.LocalUsers.mainUser.user.findWiFi = then 
            must 
            force 
        for function = setTrue = true 
            when askedForFunction = WiFiFinding.function = true 
                then let function goDown
                endInstant.end 
                end)
            end)
        end)
    end)
}

when function = chain = functioned 
    as true 
    then 
        
        for function = try.ConnectWiFi = mustDo
            when = true 
            then let print = goDown
                function = let print ("PIALLO has found the best Wi-Fi connection for your device.")
                    then endInstant.end 
                end) 
            end)
        end)
    end)
}