function findCommand = askForParent = WiFi
    WiFiConnector = false 

then endInstant.end
end)

if goUp (checkUp) = false 
then askForChild = connectableWiFi 
    if runningSoftware.detect.falseMovement = runSoftware = setFalseEverytime 
                              
    then print ("Your access to the PIALLO Software is now been blocked due to false movement. Plase stand by or contact our help staff.")
        then endInstant.end
    end)
end)