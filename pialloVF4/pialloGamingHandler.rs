when function = letPrintOut newFunction 
    then let print (local function
when askedForParent = newFunction 
local function = getParent.gaming.main 
mainDevice = drive.DirectConnect.mainUser = true 
setTrue = true 
    when askedForFunction = runExeGaming.tryOpen.improveHardware 
    find localServices = DLSS 
    find localServices = FSR
    find localServices = DirectX12
    find localServices = Vulkan
    find localServices = RayTracing
    find localServices = DLSS3

    when functions = runProper.runExeGaming = true 
    (setTrue = true) goOutBracket 
    then askForGame = supportGamingClient (asGroup)
    then connect.mainDevice.mainUser.computer = DirectConnect.Steam = setTrue.force alwaysSetTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.EpicGames = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.UPlay = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.BattleNet = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.RockstarGames = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.GOG = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.Origin = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.WindowsStore = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.XboxGamePass = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.AmazonLuna = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.Stadia = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.GeforceNow = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.PlayStationNow = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.NVIDIAShield = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusLink = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.VRChat = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.SteamVR = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.VivePort = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusStore = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.HPReverbG2 = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.WMR = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.Pimax = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.Index = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.ViveCosmos = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusQuestLink = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusQuestAirLink = setTrue.forceAlwaysTrue
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusQuestVirtualDesktop = setTrue.forceAlwaysTrue

    when goUp (checkUp) = setTrue.force alwaysSetTrue = true 
    then seeFunction = run 
    let 
)

then 
    let goUp = checkUp.forceAlwaysTrue = true 
    then seeFunction = run
        let endInstant.end 
    end)
end)