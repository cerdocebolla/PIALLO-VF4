local function
when askedForParent = newFunction 
local function = getParent.gaming.main 
mainDevice = drive.DirectConnect.mainUser = true 
setTrue = true 
    when askedForFunction = runExeGaming.tryOpen.improveHardware 
    find localServices = DLSS 
    find localServices = FSR
    find localServices = DirectX12
    find localServices = Vulkan
    find localServices = RayTracing
    find localServices = DLSS3

    when functions = runProper.runExeGaming = true 
    (setTrue = true) goOutBracket 
    then askForGame = supportGame
    then connect.mainDevice.mainUser.computer = DirectConnect.Steam = setTrue.force alwaysSetTrue = true 
    then connect.mainDevice.mainUser.computer = DirectConnect.EpicGames = setTrue.forceAlwaysTrue = true 
    then connect.mainDevice.mainUser.computer = DirectConnect.UPlay = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.BattleNet = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.RockstarGames = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.GOG = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.Origin = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.WindowsStore = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.XboxGamePass = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.AmazonLuna = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.Stadia = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.GeforceNow = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.PlayStationNow = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.NVIDIAShield = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusLink = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.VRChat = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.SteamVR = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.VivePort = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusStore = setTrue.forceAlwaysTrue  = true
    then connect.mainDevice.mainUser.computer = DirectConnect.HPReverbG2 = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.WMR = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.Pimax = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.Index = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.ViveCosmos = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusQuestLink = setTrue.forceAlwaysTrue = true 
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusQuestAirLink = setTrue.forceAlwaysTrue = true
    then connect.mainDevice.mainUser.computer = DirectConnect.OculusQuestVirtualDesktop = setTrue.forceAlwaysTrue = true

     when GoUp = CheckUp (ReadUp) = force = true 
     then let 
     static gaming = true 
    let decrease = force = true (Allow)
    then endInstant.end 
    end)
}