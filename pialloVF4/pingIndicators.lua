local function.forceToTrue = functioningDevce 
    then askForParent.force = localServers.connectableWiFi = giveInfos 

        goUp (checkUp) forceToTrue = true 
        then askForChild = connection.force = true 
        else print("No connection available for the PIALLO Software to test your ping/ms. Please try again.")
then endInstant.end
        end)
    end)
end)
            function.device.Connectors.connection.connectableWiFi = setClient 
                true 
                then askForChild.forced = connection if 

            
                if true 
                then dependUser.request.force.connection = pendChild 
                    child = connection (setTrueEverytime) = true 
                    pendConnection = setTrueEverytime
                    then print ("Calculating your ping/ms rate...")
    then 
        done.force = forceToTrue 
        then enoughData = setTrueEverytime 
            then print ("You ping/ms rate is; -ping rate-.")

                if indicator = setTrueEverytime 
                then askPermission = setTrueEverytime.mainDevice.mainUser.setAnswer.setClient.force = setTrueEverytime (forceToTrue)
                then print ("Would you want to see your ping/ms rate everytime you enter PIALLO?"))
                    and addAnswer 
    then print ("Yes") = setAnswer = asTrue = setTrueEverytime 
    then mainDevice.mainUser.mainUserPermission.force = forceToTrue 
    else 
        then print ("No") = setAnswer = false = setFalseEverytime
            then askPermission = setFalseEverytime.mainDevice.mainUser.setAnswer.setClient.force = setFalseEverytime (forceToFalse)
                enoughData.force = forceToTrue
                then showData.indicator.pingRate.msRate.localDevice = mainUser
    then endInstant.end 
                end)
            end)
        end)
    end)
end)