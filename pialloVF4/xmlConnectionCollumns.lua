local function 
    when askedForFunction 
    setParent = 1.2.0.1 (readDown.apply.FORCE::)
    <<then>: askedForDevice.usual = true.force 
        when setName.done.FORCE = true.force goDown 
        then setName (ID.code
        <<>>) versionID = Version ID 
        versiondID = 1.2.0.1
        then  endInstant.endInstant 
        end
    end)
}