local function.menu.mainDevice.mainMenu.force = setTrueEverytime (forceToTrue)
    then chooseOptions.localServers.localClient.setClient as forceToTrue |
        then enoughData = endInstant.end
    end)
end)

if 
local findCommand = pialloSetRun = false 
then print ("Activate PIALLO")
    print ("Find Source")
    print ("Settings")
    print ("Quit PIALLO")
    print ("Restart PIALLO")

    if clickDetector.mainDevice.detectMovement.click then askForParent = touchScreen.force forceToTrue (setTrueEverytime)
        then askPermission.mainMenu.mainDevice.force = true 
            setTrueEverytime = true (forceToTrue)
            when enoughData.mainDevice.detectMovement.click = true 
            then endMission.endMovement.endDetection.endInstant.end 
        end)
    end)
end)

else 
    then print ("Please choose an option, or touch the screen to unlock the screen if you can't access the menu.")
        then endMission.endMovement.endDetection.endInstant.end
    end)
end)