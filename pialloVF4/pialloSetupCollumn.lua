local function 
    runSoftware = setTrueEverytime 
    then print ("Running the PIALLO Software, please be patient.")
askForChild = second ("10,0 or 20,0") 
then askForParent = currentTime 
    currentTime.localServers.location.mainUser 
    askForParent.mainUserPermission.permission 
    then print ("Please let the PIALLO Software know your current local time for a better experience.")
        if mainUser.permission = true 
        then setTrueEverytime = true 
        else mainUser.permission = false 
            then setFalseEverytime = true 
                if mainUserPermission.permission = true 
                and location.askForParent = currentTime (enoughData)
                then print ("The Piallo Software is now running successfully, enjoy.")

                    then goUp (checkUp)
                        and readUp 
    then endInstant.end 
                    end)
                end)
            end)
        end)
    end)
end)         