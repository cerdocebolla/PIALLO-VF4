local function = true 
when setTrue = findingWiFi = goDown
must.force = true 
then function = print 
let print ("The PIALLO Software has found the best Wi-Fi connection for your device. Please wait for PIALLO to set itself up.")
    then askForFunction = Location.FindingWiFi = must.force = true 
    for function = findingWiFi GoDown (ReadDown.Read)
    then for FunctioningWiFi = must.Force = AskForSignal then 
    CreateParent.new = true 
            set Parent.SideEffect.Name = signal GoDown 
            GoUp (ReadUp) = true 
            must.Force 
        to FindSignal = depend 
    on location = activatePIALLO = pinning 
    when LocalChild = pinning = true 
    then let function endInstant.end 
    end)
}